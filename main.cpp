#include "stdio.h"
#include "math.h"
#include <iostream>
#include <vector>
using std::cout;
using std::endl;
int main() {
    std::vector<double> v, u = {1,2}, t;
    v.push_back(2.0);
    v.push_back(3.0);
    u[0] += v[0];
    u[1] += v[1],
    cout << u[0] << endl;
    cout << u[1] << endl;
    sqrt()
    return 0;
}